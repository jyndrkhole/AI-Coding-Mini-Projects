services:
  portal:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
      DATABASE_PATH: /app/data/webhook-events.db
    volumes:
      - webhook-data:/app/data
    restart: unless-stopped

volumes:
  webhook-data:
