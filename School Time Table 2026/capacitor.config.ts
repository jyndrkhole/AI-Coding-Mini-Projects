import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.schooltimetable.buddy',
  appName: 'Hey Buddy Bag Packer',
  webDir: 'dist'
};

export default config;
